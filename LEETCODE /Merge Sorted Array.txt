88. Merge Sorted Array
❓ What is the question asking?

You are given:

nums1 of size m + n
First m elements are sorted.
Last n elements are 0s (empty spaces).
nums2 of size n, also sorted.

Your task is to merge nums2 into nums1 so that the final nums1 remains sorted.

Example
Input:
nums1 = [1,2,3,0,0,0]
m = 3

nums2 = [2,5,6]
n = 3

Output:
[1,2,2,3,5,6]
🪨 Brute Force Approach
💡 Idea
Copy all elements of nums2 into the empty positions of nums1.
Sort the entire nums1.
💻 Java Code
import java.util.Arrays;

class Solution {

    public void merge(int[] nums1, int m, int[] nums2, int n) {

        for (int i = 0; i < n; i++) {
            nums1[m + i] = nums2[i];
        }

        Arrays.sort(nums1);
    }
}
▶️ Output
[1,2,2,3,5,6]
⏱️ Time Complexity

Copying → O(n)

Sorting → O((m+n) log(m+n))

Overall:

O((m+n) log(m+n))

📦 Space Complexity

O(1)

⚡ Optimal Approach
💡 Idea

Both arrays are already sorted.

Instead of merging from the beginning, merge from the end.

Use three pointers:

i → Last valid element of nums1
j → Last element of nums2
k → Last position of nums1

At each step:

Compare nums1[i] and nums2[j].
Place the larger element at nums1[k].
Move the corresponding pointer.
Decrement k.

After one array finishes, copy any remaining elements from nums2.

We don't need to copy remaining elements from nums1 because they are already in the correct position.

🧠 Dry Run
nums1 = [1,2,3,0,0,0]
nums2 = [2,5,6]

i = 2
j = 2
k = 5
nums1[i]	nums2[j]	Place	nums1
3	6	6	[1,2,3,0,0,6]
3	5	5	[1,2,3,0,5,6]
3	2	3	[1,2,3,3,5,6]
2	2	2	[1,2,2,3,5,6]
1	-	Done	[1,2,2,3,5,6]
💻 Java Code
class Solution {

    public void merge(int[] nums1, int m, int[] nums2, int n) {

        int i = m - 1;
        int j = n - 1;
        int k = m + n - 1;

        while (i >= 0 && j >= 0) {

            if (nums1[i] > nums2[j]) {
                nums1[k] = nums1[i];
                i--;
            } else {
                nums1[k] = nums2[j];
                j--;
            }

            k--;
        }

        while (j >= 0) {
            nums1[k] = nums2[j];
            j--;
            k--;
        }
    }
}
▶️ Output
[1,2,2,3,5,6]
⏱️ Time Complexity

O(m + n)

📦 Space Complexity

O(1)

📝 Key Interview Insight
Both arrays are already sorted.
Merging from the front would overwrite valid elements in nums1.
Merging from the back avoids overwriting because the empty spaces are at the end.
Use three pointers:
i → Last valid element of nums1
j → Last element of nums2
k → Last position in nums1
Copy remaining elements from nums2 if any.
No need to copy remaining elements from nums1.
🎯 Pattern
✅ Two Pointers
✅ Merge Two Sorted Arrays
✅ In-place Array Manipulation
🔑 Quick Revision
Start from the end, not the beginning.
Compare nums1[i] and nums2[j].
Place the larger element at nums1[k].
Move the corresponding pointer.
Copy remaining elements of nums2.
Final Complexity
⏱️ Time: O(m + n)
📦 Space: O(1)
