100. Same Tree / Check if Two Trees are Identical 💖
❓ What is the question asking?

Check whether:

two binary trees are identical

or not.

🌟 Two Trees are Identical if:
✅ Conditions

1️⃣ Structure must be same
2️⃣ Node values must be same

🌳 Example of Identical Trees

Tree 1:

        1
       / \
      2   3

Tree 2:

        1
       / \
      2   3

Answer:

true
❌ Example of Non-Identical Trees

Tree 1:

        1
       / \
      2   3

Tree 2:

        1
       / \
      3   2

Answer:

false
🌟 Main Idea 💡

At every node:

Check:

1️⃣ Both nodes are null ✅
2️⃣ One node null ❌
3️⃣ Values different ❌
4️⃣ Recursively check:

left subtree
right subtree
⚡ Optimal Recursive Approach
💡 Idea

Compare nodes simultaneously.

💻 Java Code
class Solution {

    public boolean isSameTree(TreeNode p, TreeNode q) {

        // Both NULL
        if (p == null && q == null)
            return true;

        // One NULL
        if (p == null || q == null)
            return false;

        // Values different
        if (p.val != q.val)
            return false;

        // Check LEFT and RIGHT subtree
        return isSameTree(p.left, q.left) &&
               isSameTree(p.right, q.right);
    }
}
▶️ Output
true

(for identical trees)

🌟 Dry Run

Suppose:

        1
       / \
      2   3

and

        1
       / \
      2   3
Compare 1 and 1
same ✅

Go left & right.

Compare 2 and 2
same ✅
Compare 3 and 3
same ✅
All checks passed

Return:

true

DONE ✅

🌟 Important Cases
Case 1
NULL + NULL

Return:

true

Because both trees ended together.

Case 2
NULL + node

Return:

false

Structure mismatch ❌

Case 3
values different

Return:

false
⏱️ Time Complexity
Complexity	Value
Time	O(n)

Every node checked once.

📦 Space Complexity
Complexity	Value
Space	O(n)

Recursion stack.

🌟 Iterative Approach (Using Queue)
💡 Idea

Compare nodes level by level.

💻 Java Code
import java.util.*;

class Solution {

    public boolean isSameTree(TreeNode p, TreeNode q) {

        Queue<TreeNode> queue = new LinkedList<>();

        queue.offer(p);
        queue.offer(q);

        while (!queue.isEmpty()) {

            TreeNode first = queue.poll();
            TreeNode second = queue.poll();

            if (first == null && second == null)
                continue;

            if (first == null || second == null)
                return false;

            if (first.val != second.val)
                return false;

            queue.offer(first.left);
            queue.offer(second.left);

            queue.offer(first.right);
            queue.offer(second.right);
        }

        return true;
    }
}
⏱️ Complexity
Complexity	Value
Time	O(n)
Space	O(n)
🎀 Memory Trick

Two trees are same if:

Structure same
AND
Values same
